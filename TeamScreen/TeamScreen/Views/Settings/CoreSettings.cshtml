@model TeamScreen.Models.Settings.CoreSettings

<form asp-controller="CoreSettings" asp-action="Save" method="post">
    <div class="form-group">
        <label for="interval">Plugin change interval in seconds</label>
        <input type="number" class="form-control" id="interval" value="@Model.Interval">
    </div>
    <button type="submit" class="btn btn-default">Submit</button>
</form>